# Dogfooding reports

Follow `../DOGFOODING.md`.

The first mandatory v0.1 report must be named:

`YYYY-MM-DD-techvit.md`

It must document a real TechVit-owned, non-confidential corpus and the complete ingest → question → Golden Dataset → evaluate → improve → compare → deploy → Runtime API → revoke flow.
